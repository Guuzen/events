{
  "type": "object",
  "title": "money",
  "description": "money",
  "additionalProperties": false,
  "properties": {
    "amount": {
      "type": "string",
      "pattern": "^\\d+"
    },
    "currency": {
      "type": "string",
      "enum": [
        "RUB"
      ]
    }
  },
  "required": [
    "amount",
    "currency"
  ]
}