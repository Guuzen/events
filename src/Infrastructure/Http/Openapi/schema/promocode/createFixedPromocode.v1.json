{
  "title": "createFixedPromocode",
  "type": "object",
  "description": "",
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid"
    },
    "code": {
      "type": "string"
    },
    "discount": {
      "$ref": "../money.v1.json"
    },
    "use_limit": {
      "type": "number"
    },
    "usable": {
      "type": "boolean"
    },
    "allowed_tariff_ids": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      }
    },
    "expire_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "event_id",
    "code",
    "discount",
    "use_limit",
    "usable",
    "allowed_tariff_ids",
    "expire_at"
  ]
}
