{
  "title": "order",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "event_id": {
      "type": "string",
      "format": "uuid"
    },
    "tariff_id": {
      "type": "string",
      "format": "uuid"
    },
    "paid": {
      "type": "boolean"
    },
    "price": {
      "$ref": "../money.v1.json"
    },
    "discount": {
      "nullable": true,
      "type": "object",
      "properties": {
        "type": {
          "type": "string"
        },
        "amount": {
          "anyOf": [
            {
              "$ref": "../money.v1.json"
            }
          ]
        }
      },
      "required": [
        "type",
        "amount"
      ]
    },
    "cancelled": {
      "type": "boolean"
    },
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "maked_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "event_id",
    "tariff_id",
    "paid",
    "price",
    "cancelled",
    "user_id",
    "maked_at"
  ]
}
