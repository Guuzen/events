{
  "title": "order",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "event_id": {
      "type": "string",
      "format": "uuid"
    },
    "tariff_id": {
      "type": "string",
      "format": "uuid"
    },
    "paid": {
      "type": "boolean"
    },
    "sum": {
      "$ref": "../money.v1.json"
    },
    "discount": {
      "nullable": true,
      "anyOf": [
        {
          "$ref": "../money.v1.json"
        }
      ]
    },
    "cancelled": {
      "type": "boolean"
    },
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "maked_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "event_id",
    "tariff_id",
    "paid",
    "sum",
    "cancelled",
    "user_id",
    "maked_at"
  ]
}
